import * as React from 'react'
import { Input } from 'antd'

export default class SignIn extends React.Component {
  render () {
    return (
      <section>
        <span>SignIn</span>
        <Input/>
      </section>
    )
  }
}